import { DateValueAcessorDirective } from './date-value-acessor.directive';

describe('DateValueAcessorDirective', () => {
  it('should create an instance', () => {
    const directive = new DateValueAcessorDirective();
    expect(directive).toBeTruthy();
  });
});
